Continue from the latest committed state.

Implement Performance Phase B only.

Goals:
1. Reduce unnecessary rerenders.
2. Stabilize analytics/chart rendering.
3. Reduce animation/render cost on lower-end devices.
4. Improve render consistency for large dashboard components.

Do not touch:
- backend
- database schema
- auth
- SSE architecture
- inventory locking
- order/payment business logic
- pagination logic
- route splitting architecture
- responsive layout system

Focus only on:
- analytics components
- dashboard rendering
- chart components
- expensive list rendering
- unnecessary rerenders
- render stability

Requirements:

1. Analytics optimization
- Memoize expensive chart transformations.
- Prevent chart rerenders when unrelated dashboard state changes.
- Use React.memo only where it provides measurable value.
- Keep chart behavior visually identical.

2. Rendering optimization
- Identify components re-rendering unnecessarily.
- Stabilize props/callbacks only where beneficial.
- Avoid blanket useCallback/useMemo spam.
- Keep code readable.

3. Animation optimization
- Reduce unnecessary Framer Motion rerenders.
- Prevent animation remounts where avoidable.
- Preserve smooth UX.
- Keep animations subtle and operationally fast.

4. Dashboard stability
- Reduce expensive rerenders in:
  - analytics-focus-dashboard
  - compact analytics cards
  - orders-manager
  - inventory-manager
- Preserve current UI/UX behavior.

5. Mobile performance
- Reduce render cost on mobile/tablet.
- Avoid unnecessary animation work on smaller screens.
- Preserve responsiveness.

Before editing:
1. identify real performance bottlenecks vs fake optimizations
2. identify components causing the most rerenders
3. explain optimization strategy
4. explain what should NOT be optimized yet

After implementation:
1. files changed
2. measurable optimization areas
3. rerender reductions
4. chart/render improvements
5. animation improvements
6. manual performance test steps
7. remaining bottlenecks
8. future optimization recommendations

Do not implement virtualization yet.
Do not add new dependencies.
Do not overengineer abstractions.
Keep changes incremental and reviewable.