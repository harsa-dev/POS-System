Continue from the latest committed state.

Implement a final UI color consistency pass only.

Goals:
1. Unify the app color system.
2. Remove inconsistent hardcoded black/gray styling.
3. Make all pages visually feel like one cohesive product.
4. Preserve the existing blue global theme from globals.css.

Do not touch:
- backend
- business logic
- auth
- realtime logic
- inventory logic
- payment logic
- accessibility behavior
- responsive architecture
- animations
- routing

Focus only on:
- visual color consistency
- theme token usage
- card/background consistency
- text color consistency
- border consistency
- hover/focus consistency

Requirements:

1. Use global theme tokens
- Reuse existing colors from globals.css.
- Prefer CSS variables/theme tokens over hardcoded colors.
- Eliminate isolated black/neutral surfaces that visually clash with the blue theme.

2. Remove inconsistent hardcoded colors
Inspect and normalize:
- bg-black
- text-black
- border-black
- overly dark neutral cards
- inconsistent gray shades
- inconsistent hover colors
- inconsistent focus ring colors

3. Preserve hierarchy
Do NOT flatten all contrast.
Keep:
- destructive actions red
- success green
- warning amber
- informational blue
But normalize their tone to match the app's existing visual language.

4. Component consistency
Normalize:
- cards
- modals
- sheets
- dropdowns
- tables
- topbar
- sidebar
- analytics widgets
- skeletons
- empty states
- status badges

5. Maintain readability
- Preserve accessibility contrast.
- Avoid low-contrast “washed out SaaS” styling.
- Keep operational readability high.

6. Keep styling incremental
Do not redesign layouts.
Do not rewrite Tailwind structure unnecessarily.
Keep changes reviewable and low-risk.

Before editing:
1. identify inconsistent color sources
2. identify highest-contrast visual clashes
3. explain normalization strategy
4. identify any areas that should intentionally remain visually distinct

After implementation:
1. files changed
2. color inconsistencies fixed
3. theme consistency improvements
4. components normalized
5. remaining intentional color differences
6. manual visual QA checklist

Do not add dark mode.
Do not redesign branding.
Do not change spacing/layout systems.