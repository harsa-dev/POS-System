he following implementation is to compare the 4 existing business modes and read the available docs, at this stage there are no code changes, just scanning the code, the expected output is the comparison results, the progress of each business mode, how the flow of each business mode is, what needs to be fixed, edge cases that might occur, what the next plan is. 6 minutes ago Analyzing business mode functionality I'll do a deep scan of all 4 business modes in parallel. Let me explore the codebase comprehensively. 7 actions Now let me do deeper targeted reads on the specific areas that need investigation — the mode contracts, routing guards, inventory policies, and frontend module registry — to get the full edge case picture. 10 actions Let me read a few more targeted files to complete the analysis. 5 actions Let me grab a couple more targeted files to complete the picture before writing the analysis. 5 actions Now I have everything I need for a thorough analysis. Here's the full comparison: Business Mode Comparison Report Overview Dimension	🍽️ Restaurant	🛒 Retail	🌾 Raw Material	🔧 Custom Business Status	available (default)	available	available	planned (guarded) Selectable	✅ Yes	✅ Yes	✅ Yes	❌ No Entry Route	/workspace/restaurant/pos	/v3/retail/cashier	/v3/raw-material/kandang	/select-mode (fallback) Frontend	Fully operational	Partially wired (mixed real + mock)	Partially wired (preview-mode UI)	Blueprint placeholder only Backend API	Fully operational	Fully operational	Fully operational	Fully operational DB Schema	Complete	Complete	Complete	Complete Seed Data	✅ Full	✅ Full	✅ Full	✅ Full Module Count	7 (all active)	14 (all status: active)	7 (all active)	3 (all status: planned) Mode-by-Mode Breakdown 🍽️ 1. Restaurant — Most Complete Progress: ~90% production-ready The restaurant mode is the system's flagship and most complete mode. It was built first and everything else was modeled after it. Full Order Flow: POS Cart (item selection) → Table Selection (DINE_IN) or skip (TAKEAWAY) → POST /restaurant/orders (creates Order) → CASH: status = PAID immediately → NON-CASH: status = PENDING_PAYMENT → POST /restaurant/payments/confirm → PAID → On PAID: deductRecipeStock() fires (decrements InventoryItem quantities via Recipe links) → Kitchen Queue (PAID → PREPARING → READY) → Serving Queue (READY → SERVED) → SERVED → COMPLETED (auto-sets DiningTable status to CLEANING) Active Modules: Cashier (POS), Kitchen (KDS), Serving, Tables, Menu, Recipes, Orders Shared Modules Available: Cashflow, Financial Reports, Customers & Partners, Invoice Generator, Cashier Shift Reports What needs fixing: Recipe deduction is all-or-nothing — if partial ingredients are out of stock, the order still succeeds and silently creates negative inventory No guard at checkout to warn if ingredient stock is insufficient before placing the order Table CLEANING → AVAILABLE transition must be done manually; no timeout or auto-reset PENDING_PAYMENT orders are not visible in the kitchen queue, so kitchen staff have no awareness of unconfirmed orders that may soon appear Payment reversal route exists (restaurant-payment-reversal.ts) but there is no visible UI trigger for it in the Cashier workspace Edge Cases: Two cashiers placing an order on the same table simultaneously (no table lock/optimistic lock on OCCUPIED check) Cancelling a PAID order after stock has already been deducted — reversal of inventory is not automatic Zero-price menu items (e.g., complimentary items) flow through the payment gate and can create confusing payment records Service rate + tax rate stacked — rounding not normalized; potential cent drift on large orders Order created with DINE_IN + no table selected (possible if table list is empty) — API doesn't hard-reject this 🛒 2. Retail — Wired Backend, Mixed Frontend Progress: ~65% — backend is solid, frontend is partially mocked The retail mode has a complete, well-designed backend (real DB, real stock deduction, real payments) and a full module registry of 14 modules. However, the frontend is a split picture: some workspaces are real API-connected (receiving), while most are still mock/interactive UI without API backing. Full Checkout Flow: Barcode scan / manual search → product lookup (GET /retail/barcode/:code) → Cart assembly → POST /retail/sales/preview (non-destructive price calc) → POST /retail/sales/checkout (atomic transaction): - Creates RetailSale + RetailSaleItems (snapshot) - Decrements RetailProduct.currentStock - Creates RetailStockMovement (type: "out", reason: "sale") - Creates RetailPayment - Injects CashflowEntry (INCOME) via raw SQL - Writes AuditLog entry Return Flow: Find original RetailSale → POST /retail/returns/preview (calculate refund) → POST /retail/returns (creates RetailReturn + optional restock) Receiving Flow: GET /retail/receiving → list incoming POs → PATCH /retail/receiving/:id/status → triggers stock increment on RetailProduct Active Modules registered: Cashier, Product Catalog, Barcode/SKU, Receiving, Stock Opname, Shelf Management, Promotions, Customers & Loyalty, Returns & Exchanges, Staff & Shifts, Forecasting, Multi-location, Omnichannel Orders, Audit Controls What needs fixing: 10 of 14 modules are labeled in the registry as "Mock retail ... workspace" in their descriptions — they render UI but are not connected to real API endpoints The retail-workspace.tsx router only gives real API treatment to "receiving" — all other moduleId values fall through to RetailInteractiveWorkspace (which is the mock) The contract file still lists plannedModules: ["API integration", "Database schema", "Real inventory update"] — this description is outdated since the DB schema and API now exist, but the frontend wiring is the missing piece RetailStockMovement and RetailReturn flows exist in the backend but have no live UI — cashiers cannot process returns through the app today Barcode lookup (GET /retail/barcode/:code) is built but the cashier workspace uses mock product search Edge Cases: Checkout with quantity > currentStock — the backend validates this, but the mock frontend can bypass it by not calling preview first Barcode collision: two products sharing the same barcode (no unique constraint visible at DB level, only at application layer) Return after stock has been further sold — negative stock is possible if receiving hasn't restocked between sale and return RetailReceiving status update doesn't validate against currentStock floor — receiving can inflate stock on an item that was already deleted/archived 🌾 3. Raw Material — Preview-Mode UI, Real Backend Progress: ~70% — backend complete, frontend is "preview" (read-heavy, limited writes) Raw material is a supply-chain upstream mode for agri/livestock businesses. The backend is fully built out across 7 route files. The frontend renders real data but is described throughout as "preview" — most components are read-only dashboards rather than full CRUD workflows. Full Operational Flow: Supplier Intake (POST /raw-material/intakes) → Weighing Station (POST /raw-material/weighing — gross/tare/net) → Batch/Lot Creation (POST /raw-material/batches) → Storage Assignment (dry / cold storage locations) → Processing Run (transformation, yield tracking) └─ OR → Kandang/Pen consumption (livestock feed/use) Active Modules: Supplier Intake, Weighing, Batches, Storage, Processing, Kandang (Pen), Suppliers What needs fixing: No cashflow/financial integration — raw material purchases don't auto-write a CashflowEntry (unlike retail which does via raw SQL) No invoice module support — the shared Invoice Generator is explicitly excluded from raw-material mode (nonRawMaterialModes) Stock movement consumption from kandang to processing — the transition logic exists in the backend but the frontend write actions are preview-only Processing run "yield" (output product after transformation) has no linkage to the shared inventory model; it's siloed in RawMaterialStockMovement only The Financial Reports shared module is also excluded for this mode — operators cannot see P&L for raw material operations Edge Cases: Weighing tare > gross weight (bad scale input) — no server-side validation guard found Batch lot split across multiple storage locations — the model supports this, but the frontend has no split-batch UI Processing run started before intake is finalized — intake status states (pending, received, rejected) don't block processing run creation RawMaterialKandangPen deletion when active batches are assigned to it — referential integrity depends on cascade rules that need verification 🔧 4. Custom Business (Service) — Blueprint Progress: ~30% — backend complete, frontend is intentionally a placeholder Custom Business is the system's fourth mode, designed for service-based businesses (HVAC, IT support, calibration labs, etc.). The backend is fully designed and the schema is rich. The frontend workspace renders a single "planned" banner and links back to the mode selector. Intended Flow (designed, not yet wired): Request Intake (ServiceRequest) → Job Planning (ServiceJob — assign technician, timeline) → Cost Lines (ServiceCostLine — parts + labor) → Quotation (ServiceQuotation — customer approval) → Work Execution + Checklist (ServiceChecklistItem) → ServiceInvoice generation → Payment → mark invoice PAID → Reversal if needed (service-business-reversal.ts) Backend routes exist for: Jobs, Requests, Cost Lines, Quotations, Invoices, Workflow transitions, Reversal, Previews What needs fixing (to activate this mode): The workspace is a static placeholder — custom-business-service-workspace.tsx renders nothing interactive All 3 registered modules (registry, feature-flags, config) are status: "planned" — none are active Mode itself is isSelectable: false, so users can't enter it even if they knew the URL The service-specific frontend components exist in app/workspace/custom-business/service/ (Job Cards, Workflow Pipelines, Pricing Panels) but are not mounted anywhere in the router No shared cashflow integration in the backend service routes (unlike retail which injects CashflowEntry) Edge Cases (when activated): A ServiceInvoice linked to a cancelled ServiceJob — cancellation propagation rules aren't enforced at the DB level ServiceQuotation approved after the job has already started — no state guard visible Multiple cost lines with currency mismatch (if multi-currency is ever added) Reversal of a paid invoice when work has already been delivered — no business rule boundary defined Cross-Mode Analysis Shared Infrastructure Matrix Shared Module	Restaurant	Retail	Raw Material	Custom Business Customers & Partners	✅	✅	✅	✅ (planned) Cashflow	✅	✅	❌ excluded	✅ (planned) Invoice Generator	✅	✅	❌ excluded	✅ (planned) Financial Reports	✅	✅	❌ excluded	✅ (planned) Cashier Shift Reports	✅	✅	❌	❌ Employees	✅	❌ excluded	❌ excluded	✅ (planned) Attendance	✅	❌	❌	✅ (planned) Mode Switching Mechanics Stored in localStorage as currentBusinessMode Legacy key migration handled (fnb → restaurant, warehouse → raw-material, service → custom-business) On switch: queryClient.clear() fires to flush stale cross-mode data x-business-mode header is sent with every API request; backend requireBusinessMode middleware validates it Route guard enforces prefix isolation per mode — a retail URL is unreachable while in restaurant mode What Needs to Be Fixed (Prioritized) Priority	Mode	Issue 🔴 High	Restaurant	No pre-checkout ingredient stock check — orders succeed with 0 inventory 🔴 High	Restaurant	No UI for payment reversal — route exists but no trigger 🔴 High	Retail	13/14 frontend modules render mock data, not real API 🔴 High	Retail	Returns flow has no UI — backend complete 🟡 Medium	Restaurant	Race condition on table-occupied check (concurrent cashiers) 🟡 Medium	Restaurant	PENDING_PAYMENT orders invisible to kitchen 🟡 Medium	Raw Material	No cashflow write on material purchase intake 🟡 Medium	Raw Material	Processing yield not linked to shared inventory 🟡 Medium	Custom Business	Frontend workspace is a static placeholder — needs mounting existing service components 🟢 Low	Retail	Contract plannedModules list is stale (schema + API exist) 🟢 Low	Raw Material	Weighing has no server-side validation for tare > gross 🟢 Low	Custom Business	Cashflow not integrated into service invoice payment routes Next Plan (Suggested Sequence) Phase 1 — Complete Retail Frontend (highest ROI since backend is done) Wire the cashier workspace to real API (POST /retail/sales/checkout, barcode lookup) Build a live Returns desk UI connected to POST /retail/returns Connect Stock Opname to real inventory endpoints Phase 2 — Harden Restaurant Add a pre-checkout stock availability check against recipe quantities Expose the payment reversal flow in the Cashier UI Add a PENDING_PAYMENT indicator in the Kitchen queue Phase 3 — Activate Raw Material Writes Wire intake → weighing → batch creation as a real multi-step form (currently read-only preview) Inject CashflowEntry on intake confirmation (same pattern as retail checkout) Phase 4 — Activate Custom Business Mount the existing service components (Job Cards, Pipeline, Pricing) into the router Flip mode status from planned → available and isSelectable: true Wire ServiceInvoice payment to CashflowEntry Next, I can wire the Retail Cashier frontend to the real checkout API (POST /retail/sales/checkout + barcode lookup) to make the retail mode fully functional end-to-end. Would you like me to do that?